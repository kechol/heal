/target
**/*.rs.bk
Cargo.lock.bk

# IDE
.idea/
.vscode/

# macOS
.DS_Store

# HEAL local state when used inside this repo for dogfooding
/.heal/

# tmp / output dirs from CLAUDE.md conventions
/tmp/
/.output/

# Local-only design notes (kept on disk, intentionally not in git history).
/KNOWLEDGE.md
/TODO.md

# Claude Code local settings (per-machine permissions etc.)
/.claude/

# docs/ Starlight site — node_modules + build output should not be tracked
docs/node_modules/
docs/dist/
docs/.astro/
