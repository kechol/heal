---
title: heal
description: Watch your codebase decay with every commit and hand the next refactor to Claude before a human has to notice.
---

import { LinkButton } from '@astrojs/starlight/components';

`heal` watches your codebase decay between commits — complexity
creep, hotspots, duplication — and turns each finding into a TODO
your next Claude session can drain, one fix per commit. No human in
the polling path.

<LinkButton href="/heal/quick-start/" icon="right-arrow">
  Quick Start (5 min)
</LinkButton>
<LinkButton href="/heal/concept/" variant="secondary" icon="open-book">
  Read the concept
</LinkButton>

## Why heal

AI coding agents are good at producing the next change you request.
Codebases, however, evolve continuously: complexity accumulates, the
same files get touched repeatedly, and duplicated blocks drift in.
The agent doesn't observe these long-term shifts — without an
external signal, the human is the only one tracking them.

`heal` fills that gap. A git post-commit hook surfaces every Critical
/ High finding right inside the commit output, and `heal status`
hands the same findings to the bundled `/heal-code-patch` Claude
skill, which drains them one finding per commit.

Three feature families shape what heal observes:

- **Code** is always on — complexity, churn, duplication, hotspots,
  cohesion. The seven metrics every project gets after `heal init`.
- **Test** (opt-in) reads `lcov.info` from your existing reporter
  and surfaces uncovered hot paths plus drifting tests.
- **Docs** (opt-in) compares paired documentation against the source
  it describes and flags drift, broken links, and orphan pages.

See [Features](/heal/features/) for the full picture.
