---
title: HEAL
description: A code-health harness that measures your codebase on every commit and surfaces relevant changes to your AI agent.
template: splash
hero:
  tagline: A code-health harness that measures your codebase on every commit and surfaces relevant changes to your AI agent.
  actions:
    - text: Quick Start (5 minutes)
      link: /heal/quick-start/
      icon: right-arrow
      variant: primary
    - text: Read the concept
      link: /heal/concept/
      icon: open-book
    - text: View on GitHub
      link: https://github.com/kechol/heal
      icon: external
---

import { Card, CardGrid } from '@astrojs/starlight/components';

AI coding agents are good at producing the next change you request.
Codebases, however, evolve continuously: complexity accumulates, the
same files get touched repeatedly, and duplicated blocks drift in.
The agent does not observe these changes — without an external signal,
the human is the only one tracking them.

HEAL fills that gap. It measures the codebase on every commit and
surfaces the relevant changes to the next Claude Code session.

<CardGrid>
  <Card title="Six standard metrics" icon="seti:rust">
    Lines of code, complexity, churn, change coupling, duplication, and a composed hotspot score —
    computed automatically on every commit.
  </Card>
  <Card title="Hook-driven" icon="puzzle">
    A git post-commit hook writes the snapshot. A Claude SessionStart hook surfaces what changed. No
    daemon, no scheduler.
  </Card>
  <Card title="Read-only by default" icon="approve-check">
    In v0.1, HEAL only reads the repository. Automated repair arrives in v0.2 behind an explicit
    policy opt-in.
  </Card>
  <Card title="Single binary" icon="rocket">
    `cargo install heal-cli` installs the CLI. The Claude plugin is bundled in the binary and
    extracted by `heal skills install`.
  </Card>
</CardGrid>

:::caution[Status: v0.1]
macOS and Linux only. The CLI surface and on-disk schemas may change
before v0.2. Feedback is welcome; expect rough edges.
:::

## Where to start

- For background, read the [concept](/heal/concept/) — a three-minute
  overview of what HEAL is for.
- To try it, follow [Quick Start](/heal/quick-start/) — a
  five-minute install and first snapshot.
- For day-to-day use, [CLI](/heal/cli/) and
  [Configuration](/heal/configuration/) cover the common operations.
